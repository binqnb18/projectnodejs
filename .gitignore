# Ignore node_modules directory
node_modules/

# Ignore environment variable files (if you're using dotenv or similar)
.env

# Ignore log files
*.log

# Ignore system files
.DS_Store
Thumbs.db

# Ignore package-lock and yarn.lock (optional, depends on your team's workflow)
# Nếu bạn không muốn đẩy file lock lên, thì giữ dòng dưới, nếu muốn đẩy, hãy xóa chúng đi.
# package-lock.json
# yarn.lock

# Ignore coverage files (from testing tools)
coverage/

# Ignore build output (if using bundlers or compilers)
dist/
build/
